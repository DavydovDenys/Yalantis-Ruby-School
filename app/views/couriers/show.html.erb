<p id="notice"><%= notice %></p>

<div>
  <strong>
  <%= @courier.name %>
  <%= @courier.email %>
  </strong>
  <%= link_to 'Back', couriers_path %>
</div>

<%= render 'packages/form', courier: @courier %>

<table>
  <thead>
    <tr>
      <th>Tracking number</th>
      <th>Delivery status</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <%= render partial: 'packages/all', collection: @courier.packages.reverse, as: :package %>
</table>